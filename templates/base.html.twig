<!DOCTYPE html>
<html lang="en">
<head>
    <title>{% trans %} title.home {% endtrans %}</title>
</head>
<body>
<h1> {% trans %} title.home {% endtrans %} </h1>

<ul id="links">
    {% for item in links %}
        <li><a href="{{ item }}">{{ item }}</a></li>
    {% endfor %}
</ul>
</body>
</html>
