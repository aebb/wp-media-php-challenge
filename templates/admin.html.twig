<!DOCTYPE html>
<html lang="en">
<head>
    <title>{% trans %} title.admin {% endtrans %}</title>
</head>
<body>
<h1>{% trans %} title.admin {% endtrans %}</h1>
<ul id="crawl">
    {% if task is null %}
        <p>{% trans %} admin.no_crawl {% endtrans %}</p>
    {% else %}
        <p> {{ task }}</p>
        {% for item in task.taskItems %}
            <li><a href="{{ item }}">{{ item }}</a></li>
        {% endfor %}
    {% endif %}
</ul>
<form action="{{ path('admin.post') }}" method="post">
    <button type="submit">{% trans %} admin.new_crawl {% endtrans %}</button>
</form>
</body>
</html>
